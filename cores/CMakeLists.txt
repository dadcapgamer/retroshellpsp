# Emulator cores. Each subdirectory produces one core via rs_add_core().
# The dummy core (Phase 3) validates the Core API and the PRX/static
# loading pipeline; real emulator cores land in later phases.

# add_subdirectory(dummy)   # enabled in Phase 3
